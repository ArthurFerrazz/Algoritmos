/*Faça o mesmo exercício, atribuindo valor às variávies A e B (não é necessário
solicitar ao usuário, os valores serão atribuídos no próprio código)*/

#include<stdio.h>

int main(){
	int A, B, X;
	
	A = 25;
	B = 25;
	X = A + B;
	printf("A soma entre os valores eh: %d", X);
}
